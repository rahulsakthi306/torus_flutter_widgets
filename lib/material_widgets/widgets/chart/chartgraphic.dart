import 'package:flutter/material.dart';
import 'package:graphic/graphic.dart';

class Chartgraphic extends StatelessWidget {
  const Chartgraphic({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        // Graphic()
      ],
    );
  }
}